# Harness durable layer — each project instance generates its own data.
harness.db
harness.db-wal
harness.db-shm

# Rust build output for harness tooling.
target/
dist/

# Downloaded Harness CLI binary for installed project instances.
scripts/bin/harness-cli
scripts/bin/harness-cli.exe

# Frontend (TanStack Start) — dependencies, build output, generated files.
node_modules/
package-lock.json
.output/
.nitro/
.tanstack/
.vite/
.env
.env.local
# Auto-generated by the TanStack Router/Start Vite plugin.
src/routeTree.gen.ts

# Playwright e2e artifacts.
test-results/
playwright-report/
blob-report/
.playwright/

# Design handoff bundles (kept locally, not committed)
*handoff*.zip
